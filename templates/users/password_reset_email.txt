Hi {{ user_name }},

We received a request to reset your password for {{ tenant_name|default:"SmartCare HMS" }}.

Your reset token:
  {{ reset_token }}

This token will expire in {{ expiry_hours }} hour.

Open the account recovery link below. The token will already be populated; click Verify token before creating and confirming your new password.

{% if reset_url %}
Account recovery link:
  {{ reset_url }}
{% endif %}

If you didn't request a password reset, you can ignore this email. Your account remains secure.

{{ tenant_name|default:"SmartCare HMS" }}
